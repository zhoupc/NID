classdef NID < handle 
    
    properties 
        net = []; 
        net_name = 'net_17'; 
        iter = 2; 
    end 
    
    
    methods 
        function obj = NID(net_name, iter)
            nid_folder = fileparts(mfilename('fullpath'));
            net_file = fullfile(nid_folder, 'log
            obj.net = importONNXLayers(); 
        end 
        
        %% denoise 
        function img = denoise(obj, img)
            for m=1:obj.iter
                % denoise 
            end 
        end 
        
    end 
    
end 